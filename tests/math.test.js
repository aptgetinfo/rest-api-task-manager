const calculate=require('../src/hack')


test('Test 1',()=>{
    const total=calculate(10,.3)
    expect(total).toBe(13)
    
})

test('Test 2',()=>{
    const total=calculate(10,.3)
    expect(total).toBe(13)
    
})

test('Test 3',()=>{
    const total=calculate(10,.3)
    expect(total).toBe(13)
    
})

test('Test 4',()=>{
    const total=calculate(10,.3)
    expect(total).toBe(13)
    
})